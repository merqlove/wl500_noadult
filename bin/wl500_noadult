#!/usr/bin/env ruby

# resolve bin path, ignoring symlinks
require 'pathname'
bin_file = Pathname.new(__FILE__).realpath

$LOAD_PATH.unshift File.expand_path('../../', bin_file)
require 'rake'

args = "[#{ARGV.join(',')}]"

load 'Rakefile'
Rake::Task.invoke_task("filter:update#{args}")
